/** Project slug → placeholder image slot (see content/placeholders.ts). */
export const projectPlaceholder: Record<string, string> = {
  "neuro-adaptive-game": "project-neuro",
  "komat-unpar": "project-komat",
  "solar-monitor": "project-solar",
};
